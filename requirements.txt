pillow==10.3.0
# 1.24.x is the final NumPy series supporting Python 3.8. Python 3.8 is used
# deliberately for Windows builds because it is the final CPython series
# supporting Windows 7. Newer interpreters use the newer NumPy wheel.
numpy==1.24.4; python_version < "3.9"
numpy==1.26.4; python_version >= "3.9"
# The package metadata requires NumPy >=1.26.4, which cannot be installed on
# Python 3.8. Install homm3data separately with --no-deps on Python 3.8; the
# normal dependency resolution remains enabled on newer Python versions.
homm3data==0.1.13; python_version >= "3.9"
